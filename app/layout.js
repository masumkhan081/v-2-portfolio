import Head_nav from "./Head_nav";
import "./globals.css";

export const metadata = {
  title: "Hyped With MERN !",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-teal-800 h-full w-screen">
        <Head_nav />

        {children}
      </body>
    </html>
  );
}
